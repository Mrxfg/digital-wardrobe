name: Backend CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

defaults:
  run:
    working-directory: backend

jobs:
  lint-and-test:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install tools
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black

    - name: Lint with flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Check code formatting with black
      run: black --check .

    - name: Compile all Python files
      run: python -m compileall .

    - name: Security check - Verify no hardcoded secrets
      run: |
        ! grep -r "password\s*=\s*['\"]" . --include="*.py" >&2
        ! grep -r "secret\s*=\s*['\"]" . --include="*.py" >&2

  build-and-test:
    name: Build & Test Suite
    runs-on: ubuntu-latest
    needs: lint-and-test

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install all dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        pip install pillow-heif onnxruntime rembg pytest pytest-cov httpx pip-audit

    - name: Dependency vulnerability scan (pip-audit)
      run: |
        if [ -f requirements.txt ]; then python3 -m pip_audit -r requirements.txt; fi
        echo "✓ No known vulnerabilities found"

    - name: Create uploads directory
      run: mkdir -p uploads/original uploads/processed

    - name: Test application imports
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
        SECRET_KEY: test-secret-key-for-ci-only
        ALGORITHM: HS256
      run: python -c "from app.main import app; print('✓ Application imports successfully')"

    - name: Check database models
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
        SECRET_KEY: test-secret-key-for-ci-only
        ALGORITHM: HS256
      run: python -c "from app.database import Base, engine; Base.metadata.create_all(bind=engine); print('✓ Database models created successfully')"

    - name: Run Unit & Integration Tests
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
        SECRET_KEY: test-secret-key-for-ci-only
        ALGORITHM: HS256
        NUMBA_DISABLE_JIT: "1"
      run: pytest tests/ -v -s --cov=app --cov-report=xml --cov-report=term

    - name: Run QRT-001 (API Response Time)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
        SECRET_KEY: test-secret-key-for-ci-only
        ALGORITHM: HS256
      run: pytest tests/quality/ -v -s -k "qr001"

    - name: Run QRT-002 (Fault Tolerance)
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
        SECRET_KEY: test-secret-key-for-ci-only
        ALGORITHM: HS256
      run: pytest tests/quality/ -v -s -k "qr002"

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-report
        path: backend/coverage.xml
